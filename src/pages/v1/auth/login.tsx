import { type NextPage } from "next"
import { api } from "@/src/utils/api"
import DebugView from "@/src/components/test/debug-view"

    export const Login: NextPage = () => {
        const mutation = "something"

        return (
            <div>
                
            </div>
        )
    }